// Create array of names and convert all to uppercase.

let names = ["jayant", "kushwah", "mahi", "dishika", "hemlata", "devendra"];

let result = names.map((str) => str.toUpperCase());
console.log(result);
